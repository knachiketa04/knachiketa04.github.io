# Claude Code workspace — local only, never publish
CLAUDE.md
.claude/
.claude.json
memory/

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS junk
.DS_Store
Thumbs.db

# Secrets / credentials (defense in depth — none expected in this repo)
.env
.env.*
*.pem
*.key
*credentials*.json
*service-account*.json

# Local previews / generated artifacts
.cache/
node_modules/

# Blog build pipeline. build/ is TRACKED source (Markdown posts, templates,
# styles, build.py, COMPONENTS.md). Ignore only its local-only artifacts:
build/__pycache__/
build/drafts/
build/.pre-promote-backup/
